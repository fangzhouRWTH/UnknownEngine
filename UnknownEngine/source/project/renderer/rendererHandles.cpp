#include "rendererHandles.hpp"

namespace unknown::renderer
{
    // _HANDLEIMPL(ProgramHandle);
    // _HANDLEIMPL(TextureHandle);
    // _HANDLEIMPL(MeshHandle);

    // _HANDLEIMPL(RenderElementHandle);
}